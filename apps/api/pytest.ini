[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    integration: requires external services (OpenAI, Postgres). Skipped when OPENAI_API_KEY is not set.
